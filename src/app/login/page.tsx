import { LoginForm } from "./LoginForm";

export const metadata = { title: "Entrar · Concessionária Demo" };
export const dynamic = "force-dynamic";

export default function LoginPage() {
  return <LoginForm />;
}
